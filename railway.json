{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfigPath": "nixpacks.toml",
    "watchPatterns": ["obra10-backend/**"]
  },
  "deploy": {
    "startCommand": "cd obra10-backend && npx prisma migrate deploy && npm run start:prod",
    "restartPolicyType": "ON_FAILURE"
  }
}
